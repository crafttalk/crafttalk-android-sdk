<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/pinned_message"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:minHeight="64dp"
    android:maxHeight="72dp"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/icon_pin"
        android:layout_width="24dp"
        android:layout_height="48dp"
        android:layout_weight="0"
        android:layout_marginLeft="8dp"
        android:src="@drawable/com_crafttalk_chat_ic_keep"
        app:layout_constraintBottom_toBottomOf="@id/pinned_message_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

        <TextView
            android:id="@+id/pinned_operator_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toRightOf="@id/icon_pin"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_marginTop="8dp"
            android:layout_weight="1"
            android:text="Пользователь"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/pinned_message_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            app:layout_constraintTop_toBottomOf="@id/pinned_operator_name"
            app:layout_constraintLeft_toRightOf="@id/icon_pin"
            app:layout_constraintRight_toRightOf="parent"
            android:ems="10"
            android:gravity="start|top"
            android:maxLines="2"
            android:text="Закреплённый тест сообщения "
            tools:visibility="visible" />

    </androidx.constraintlayout.widget.ConstraintLayout>